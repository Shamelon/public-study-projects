from setuptools import setup, find_packages

setup(
    name='for tg',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'aiogram', 'googlesearch-python', 'python-dotenv'
    ],
)
